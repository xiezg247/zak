[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "vnpy-llm"
version = "0.1.0"
description = "VeighNa LLM 对话插件"
requires-python = ">=3.10"
dependencies = [
    "vnpy>=4.0.0",
    "openai>=1.0.0",
    "markdown>=3.5",
    "Pygments>=2.17",
    "python-dotenv>=1.0.0",
    "langgraph>=0.2",
    "langchain-openai>=0.2",
    "langchain-core>=0.3",
    "vnpy-common",
    "vnpy-skills",
    "vnpy-mcp",
    "langchain>=0.3",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["vnpy_llm*"]
